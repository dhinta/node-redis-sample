const HTTP_CODE = {
  inoperableEntry: 422,
  success: 200
};

const ERROR_CODE = {
  'VALIDATION_ERROR.INVALID_INPUT.11000': 'Duplicate Entry For {{USER_NAME}}'
};

export default {
  HTTP_CODE,
  ERROR_CODE
};
